"use strict";const binGo=()=>{function createColumn(col){const source=[];for(let i=0;i<15;i++)source[i]=i+1+15*col;const column=[];for(let i=0;i<5;i++)column[i]=source.splice(Math.floor(Math.random()*source.length),1)[0];return column}function createColumns(){const columns=[];for(let i=0;i<5;i++)columns[i]=createColumn(i);return columns[2][2]="FREE",columns}function renderBingo(columns){for(let row=0;row<5;row++){const tr=document.createElement("tr");for(let col=0;col<5;col++){const td=document.createElement("td");td.textContent=columns[col][row],tr.appendChild(td)}document.querySelector("tbody").appendChild(tr)}}const columns=createColumns();renderBingo(columns);const openNumber=document.getElementById("open-number"),bingoNumber=document.getElementById("bingo-number"),remainedNumber=document.getElementById("remained-number"),bingoPoint=document.getElementById("counter_point"),bingoPointBtn=document.getElementById("counter_point_btn");let currentTurn=0,isGameOver=!1,winOrNot=void 0;function setPoint(point){bingoPoint.value=point}function sendPoint(e){bingoPointBtn.click(e)}function operatePoint(){(winOrNot=!0)?setPoint(1e3):setPoint(-200),sendPoint()}openNumber.addEventListener("click",()=>{let num=Math.floor(75*Math.random()+1);bingoNumber.innerText=num,currentTurn++,remainedNumber.innerText=`${15-currentTurn}`,1==num&&xx.classList.add("disabled")}),1==num&&(winOrNot=!0,isGameOver=!0,operatePoint())};binGo();